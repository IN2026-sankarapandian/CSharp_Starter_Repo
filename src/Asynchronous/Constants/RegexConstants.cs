namespace Asynchronous.Constants;

/// <summary>
/// Have constants of regex patterns
/// </summary>
public class RegexConstants
{
    /// <summary>
    /// Allows only words and whit spaces
    /// </summary>
    public const string WordFilter = @"[^\w\s]";
}
